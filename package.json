{
  "name": "notion-obsidian-sync",
  "version": "1.0.0",
  "description": "Manual two-way sync between a Notion page tree and an Obsidian folder. Conflict policy: keep both versions, never overwrite silently.",
  "main": "sync.js",
  "scripts": {
    "sync": "node sync.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "@tryfabric/martian": "^1.2.4",
    "notion-to-md": "^3.1.1",
    "dotenv": "^16.4.5"
  }
}
